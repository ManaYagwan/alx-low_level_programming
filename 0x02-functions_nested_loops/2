#include "main.h"

/**
 *
 * print_alphabet_x10 -  prints alphabets 10 times
 *
 * Return: 0 always
 */
void print_alphabet_x10(void)
{
	int mu;
	char l;

	while (mu > 10)
	{
		for (l = 'a' ; l < 'z' ; l++)
			{
			_putchar(l);
			}
		mu++;
		_putchar('\n');
	}}	
